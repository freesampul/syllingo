import {assertSameOrigin,requireUser,readObject,adminClient,privateJson,accountFailure,HttpError} from '@/lib/auth';
export async function POST(r:Request){try{assertSameOrigin(r);const user=await requireUser();const b=await readObject(r);if(typeof b.id!=='string'||!/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(b.id)||!['idea','bug','content','other'].includes(b.kind)||typeof b.message!=='string'||b.message.trim().length<10||b.message.trim().length>3000||typeof b.reply!=='boolean')throw new HttpError(400,'Choose a category and write between 10 and 3,000 characters.');const {data,error}=await adminClient().rpc('submit_product_feedback',{p_id:b.id,p_user:user.id,p_kind:b.kind,p_message:b.message.trim(),p_email:b.reply?user.email??null:null});if(error)throw new HttpError(503,'Your feedback couldn’t be saved. Please try again.');if(!data)throw new HttpError(429,'You’ve sent 10 messages today. Thanks for the ideas—please come back tomorrow.');return privateJson({ok:true});}catch(e){return accountFailure(e)}}
export const dynamic='force-dynamic';
